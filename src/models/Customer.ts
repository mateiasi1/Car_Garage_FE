export interface Customer {
    id: string;
    firstName: string;
    lastName: string;
    phoneNumber: string;
    licensePlate: string;
    category: string;
    inspectedBy: string;
    inspectionDate: Date;
    inspectionType: string;
}